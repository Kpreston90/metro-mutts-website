# Journal Article Verification Notes

## Initial Preview — September 4, 2026

- Canonical preview route loaded with the intended browser title: “Do Dogs Need Dog Friends? A Guide to Canine Socialization | Metro Mutts Journal.”
- Header, article title, subhead, author/date/read-time metadata, internal links, source links, related articles, and primary call to action are present in the rendered page structure.
- The initial browser viewport showed unexpected blank vertical space where the supplied hero image should appear. After the image load settled, the supplied hero rendered correctly beneath the article metadata with the expected wide editorial crop.
- The canonical route exposes the intended Article title, H1, hierarchy, two source links, three internal conversion links, related articles, and the primary first-day CTA.
- The staged page has exactly one H1, eleven H2 sections, a rendered `Article` JSON-LD block, an accurate hero alt description, and a primary CTA that targets `/booking`.
- The `/booking` target was verified to redirect to the unified `/get-started` flow.
